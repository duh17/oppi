{
  "id": "748506dc",
  "title": "TRACKER: Workspace = Container migration (IMPL Phases 0-3)",
  "tags": [
    "pi-remote",
    "workspace",
    "migration",
    "tracker",
    "umbrella"
  ],
  "status": "in_progress",
  "created_at": "2026-02-08T01:24:53.254Z"
}

## Umbrella

Tracks the workspace=container migration from IMPLEMENTATION.md Phases 0-3.
Architecture: `WORKSPACE-CONTAINERS.md`. Execution: `IMPLEMENTATION.md`.

## Implementation Steps

| Step | TODO | What | Effort | Depends on |
|------|------|------|--------|------------|
| 7 | TODO-78eba302 | Workspace runtime core | 2-3d | Steps 1-6 stable |
| 8 | TODO-31efdce1 | API migration + legacy compat | 1-2d | Step 7 |
| 9 | TODO-1fe2951f | iOS workspace-first UX | 2-3d | Step 8 |
| 10 | TODO-19cb0451 | Fork workflow | 1-2d | Step 8 |

Steps 9 and 10 can run in parallel.

## Dependencies

```
Step 7 (runtime) → Step 8 (API) → Step 9 (iOS UX)
                                 → Step 10 (fork)
```

## Cross-cutting concerns (from IMPLEMENTATION.md §4)
- Fail-closed permission behavior must hold
- No regressions in toolCallId correlation
- Backward-compatible iOS decoding for at least one server release
- No destructive data migration without backup + rollback metadata

## Current state
Runtime is session-scoped containers (`<user>/<sessionId>/...`).
Workspace config exists and influences session bootstrap, but doesn't own
the container lifecycle.

## Progress
- 2026-02-09: Step 7 started. Landed workspace runtime lock/limits scaffolding (`src/workspace-runtime.ts`) and integrated it into `SessionManager` lifecycle paths.
- 2026-02-09: Landed workspace-scoped sandbox layout + legacy migration scaffolding, and updated trace/file resolution code to check workspace paths first.
- 2026-02-09: Switched runtime spawn path to workspace-owned containers (`container exec` per session into persistent workspace container) + added workspace idle container shutdown scheduling.
- 2026-02-09: **Phase 0 (TODO-78eba302) complete.** Added startup migration scan, gate port release audit + fix, gate-cleanup tests, sandbox migration tests. 15 files / 170 tests passing. Ready for Phase 1.
- 2026-02-09: **Phase 1 (TODO-31efdce1) complete.** Added workspace-scoped session API routes (`/workspaces/:wid/sessions*`), resume endpoint, deprecation headers on legacy routes, protocol version header (`X-PiRemote-Protocol: 2`), WS upgrade for both v2 and v1 paths. 16 files / 188 tests passing. Ready for Phase 2 (iOS) and Phase 3 (fork).
