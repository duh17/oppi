{
  "id": "63373415",
  "title": "TestFlight setup — need ASC API key + app creation",
  "tags": [
    "testflight",
    "ios",
    "blocked"
  ],
  "status": "blocked",
  "created_at": "2026-02-09T00:15:16.005Z"
}

Script is ready at `ios/scripts/testflight.sh`. Archive step works. Blocked on ASC credentials.

## Steps (from phone/browser)
1. https://appstoreconnect.apple.com/access/integrations/api → Generate API Key → Role: **Admin** → Download `.p8`
2. `mkdir -p ~/.appstoreconnect && mv ~/Downloads/AuthKey_*.p8 ~/.appstoreconnect/`
3. `set -Ux ASC_KEY_ID "<key-id>"` and `set -Ux ASC_ISSUER_ID "<issuer-id>"`
4. App Store Connect → My Apps → + → New App → iOS, bundle `dev.chenda.PiRemote`, SKU `piremote`
5. Run: `ios/scripts/testflight.sh --bump`

## Notes
- `-allowProvisioningUpdates` auto-creates Distribution cert — no manual cert needed
- Build dir: `ios/build/testflight-*/` (gitignored)
- First build will be slow (cert + profile provisioning)
- Rename to Oppi (TODO-c6dda8b7) should happen before first public TestFlight
