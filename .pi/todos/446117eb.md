{
  "id": "446117eb",
  "title": "iOS test: model Codable round-trips — Session, Permission, User, Workspace, TraceEvent",
  "tags": [
    "ios",
    "testing",
    "coverage"
  ],
  "status": "done",
  "created_at": "2026-02-07T21:16:38.819Z"
}

## Goal

Full encode/decode round-trip tests for all Codable models. These catch wire-format regressions when the server protocol changes.

## Files + Coverage Target

| File | Current | Exec Lines | Target |
|------|---------|------------|--------|
| Session.swift | 55% (36/65) | 65 | 90%+ |
| Permission.swift | 0% (0/24) | 24 | 90%+ |
| User.swift | 0% (0/6) | 6 | 90%+ |
| Workspace.swift | 0% (0/39) | 39 | 90%+ |
| SkillInfo.swift | 0% (0/1) | 1 | 100% |

**Estimated delta: ~80 lines newly covered**

## Test Plan

### Session
- Decode with all fields (model, contextTokens, contextWindow, lastMessage)
- Decode with minimal fields (only required)
- Unix-millisecond timestamp conversion (createdAt, lastActivity)
- Encode → decode round-trip preserves all fields
- SessionStatus enum covers all cases
- TokenUsage round-trip

### Permission
- Decode from server JSON format
- timeoutAt Unix-ms conversion
- Encode → decode round-trip
- RiskLevel enum all cases
- PermissionAction enum all cases

### User + ServerCredentials
- User decode
- ServerCredentials.baseURL — valid host, malformed host returns nil
- ServerCredentials.webSocketURL — valid, malformed returns nil

### Workspace
- Full decode with all optional fields
- Minimal decode
- Round-trip encode/decode

### TraceEvent
- Decode each type: user, assistant, thinking, toolCall, toolResult, system, compaction
- Missing optional fields default correctly

### SkillInfo
- Basic decode

## Test Files to Create/Update
- `PiRemoteTests/ModelCodableTests.swift` (new)

## Verification
```bash
cd ios && xcodegen generate && xcodebuild -project PiRemote.xcodeproj -scheme PiRemote \
  -destination 'platform=iOS Simulator,OS=26.0,name=iPhone 16 Pro' \
  -enableCodeCoverage YES test
```
