{
  "id": "745032b2",
  "title": "iOS: hardening + test coverage (NetNewsWire checklist)",
  "tags": [
    "pi-remote",
    "ios",
    "polish",
    "testing",
    "netnewswire"
  ],
  "status": "done",
  "created_at": "2026-02-07T19:16:10.562Z"
}

## Completed

### Hardening
- [x] Serialized operation queue for session transitions — added `Task.isCancelled` guards after every async boundary in `connectToSession()` to prevent stale data loading on rapid session switches
- [x] Duplicate stop-tap suppression — stop button already `.disabled(isStopping)`; added `isForceStopInFlight` guard + spinner on force-stop button
- [x] Terminal-state banner after forced stop — `reducer.appendSystemEvent("Session force-stopped")` + new `appendSystemEvent()` method on `TimelineReducer`
- [x] Remove force-unwraps — confirmed no `fatalError`/`try!`/`as!` in production paths; `baseURL`/`webSocketURL` return `URL?` with guards

### Test Coverage
- [x] Reducer edge cases (11 new tests): double agentStart, agentEnd without content, orphaned tool closed on agentEnd, toolEnd for unknown ID, toolOutput for unknown ID, events after sessionEnded, reset clears everything, processBatch with mixed events, multiple agent turns, permission expired renders as denied, appendSystemEvent
- [x] ServerMessage decode tests (14 new tests): missing type field, empty string, invalid JSON, missing required fields (textDelta, toolStart, error, sessionEnded, permissionRequest), extra fields ignored, null args defaults, isError defaults false, multiple unknown types, permissionCancelled, extensionUINotification, connected with full session fields

### Also Closed
- TODO-a1b2c311: ServerCredentials.baseURL already returns `URL?` — confirmed fixed

### Files Modified
- `ios/PiRemote/Features/Chat/ChatView.swift` — Task.isCancelled guards, isForceStopInFlight state, force-stop terminal banner
- `ios/PiRemote/Features/Chat/ChatInputBar.swift` — isForceStopInFlight param, disabled+spinner on force-stop button
- `ios/PiRemote/Core/Runtime/TimelineReducer.swift` — appendSystemEvent() method
- `ios/PiRemoteTests/TimelineReducerTests.swift` — 12 new edge-case tests
- `ios/PiRemoteTests/ServerMessageTests.swift` — 14 new decode tests
- `ios/NETNEWSWIRE_PATTERNS.md` — updated checklist items to [x]

### Test Results
68 tests in 5 suites — all passing
