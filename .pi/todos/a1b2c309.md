{
  "id": "a1b2c309",
  "title": "Make permission pill banner tappable",
  "tags": ["ios", "bug", "review-fix", "ux"],
  "status": "pending",
  "created_at": "2026-02-07T06:30:00.000Z"
}

## Problem (Review #8)

The `PermissionPillBanner` says "tap to review" but has no tap gesture. Permission cards are inline in the timeline, so the banner should scroll to the first pending permission.

## Fix

Add `onTapGesture` that scrolls to the first `.permission` item in `reducer.items` via `ScrollViewReader`.

## File

`ios/PiRemote/Features/Chat/ChatView.swift`

## Priority

Fix before first device test — UX promise not delivered.
