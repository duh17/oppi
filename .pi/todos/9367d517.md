{
  "id": "9367d517",
  "title": "iOS test: bump existing suites to 90%+ — TimelineReducer, JSONValue, ClientMessage",
  "tags": [
    "ios",
    "testing",
    "coverage"
  ],
  "status": "done",
  "created_at": "2026-02-07T21:16:53.904Z"
}

## Goal

Push files with existing partial coverage above 90%. These need targeted tests for uncovered branches only.

## Files + Coverage Target

| File | Current | Exec Lines | Gap | Target |
|------|---------|------------|-----|--------|
| TimelineReducer.swift | 74% (323/437) | 437 | ~70 lines | 90%+ |
| JSONValue.swift | 76% (51/67) | 67 | ~10 lines | 90%+ |
| ClientMessage.swift | 79% (38/48) | 48 | ~5 lines | 90%+ |
| ChatItem.swift | 89% (95/107) | 107 | ~1 line | 90%+ |
| ServerMessage.swift | 97% (104/107) | 107 | 3 lines | 97%+ (keep) |

**Estimated delta: ~86 lines newly covered**

## Test Plan

### TimelineReducer (74% → 90%)
Uncovered branches likely include:
- `loadFromTrace()` — all event type paths (user, assistant, thinking, toolCall, toolResult, system, compaction)
- `updateToolCallPreview()` when tool item not found
- `processBatch` tool output merging with existing entries
- Edge: `toolStart` with empty args map
- Edge: very long assistant text that exercises `preview()` truncation

### JSONValue (76% → 90%)
Likely uncovered:
- `.array` encode/decode
- `.null` handling
- Nested objects/arrays
- `summary()` for each variant

### ClientMessage (79% → 90%)
Likely uncovered:
- `extensionUIResponse` encoding with all field combinations (value, confirmed, cancelled)
- `getState` encoding
- `jsonString()` method

### ChatItem (89% → 90%)
- Likely just needs one more case for `timestamp` or `id` on an uncovered enum variant

## Approach
1. Run xccov with `--files-for-target` to identify exact uncovered lines
2. Write targeted tests for just those branches
3. Re-measure

## Test Files to Update
- `PiRemoteTests/TimelineReducerTests.swift`
- `PiRemoteTests/JSONValueTests.swift`
- `PiRemoteTests/ClientMessageTests.swift`

## Verification
```bash
cd ios && xcodegen generate && xcodebuild -project PiRemote.xcodeproj -scheme PiRemote \
  -destination 'platform=iOS Simulator,OS=26.0,name=iPhone 16 Pro' \
  -enableCodeCoverage YES test
```
