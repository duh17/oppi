{
  "id": "6717558a",
  "title": "P2: iOS — Skill detail view with file browser",
  "tags": [
    "pi-remote",
    "ios",
    "skills",
    "phase-2"
  ],
  "status": "backlog",
  "created_at": "2026-02-07T07:42:59.007Z"
}

## Context

Tapping a skill in the list should show its details: description, files,
and a Refine button (wired in Phase 3).

Depends on: TODO-2cd10bc4 (skills list), TODO-15956340 (file browser reuse)
Tracker: TODO-992ad1a6

## What to do

### A. SkillDetailView

New `ios/PiRemote/Features/Skills/SkillDetailView.swift`:

- Header: skill name + description (from SKILL.md frontmatter)
- "Refine" button (disabled/placeholder until Phase 3)
- Files section: reuse FileListView pointed at skill files endpoint
  (`GET /me/skills/:name/files/*path`)
- For built-in skills: show files as read-only (no delete, no refine)
- For user skills: show Delete button at bottom

### B. Wire into SkillListView

Tap a row → NavigationLink to SkillDetailView.

### C. Reuse file infrastructure

FileService needs a new method:
```swift
func skillFileURL(skillName: String, path: String) -> URL
```

FileListView should accept a generic "file source" (session workspace
OR skill storage) so it works for both contexts.

## Files

- `ios/PiRemote/Features/Skills/SkillDetailView.swift` — NEW
- `ios/PiRemote/Core/Services/FileService.swift` — add skill file URL
- `ios/PiRemote/Features/Files/FileListView.swift` — generalize source

## Done when

- Tap skill in list → see name, description, file tree
- Can tap SKILL.md → renders markdown preview
- Can tap scripts/plot.py → renders code preview
- Built-in skills show as read-only
- User skills show Delete button
