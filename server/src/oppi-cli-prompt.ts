export const OPPI_CLI_SYSTEM_PROMPT_HINT =
  "Oppi CLI: use `oppi session` to list, create, inspect, steer, wait for, and stop sessions. Session targets accept a unique Session.id prefix, like official Pi `--session` (`oppi session inspect 11111111`). Ambiguous prefixes fail. `oppi://session/<id>` and `GET /sessions/:id` stay the full UUID. `oppi session send <id> --text ...` prompts an idle session and steers a busy session; add `--follow-up` only when the message should wait until current work finishes. `--prompt @-` and `--text @-` read from stdin. Managed create/send calls are prefixed with `This is a message from session <caller-id>:`; do not type that prefix yourself.";
