export const OPPI_CLI_SYSTEM_PROMPT_HINT =
  "Oppi CLI: use `oppi session` to list, create, inspect, steer, wait for, and stop sessions. `oppi session send <id> --text ...` prompts an idle session and steers a busy session; add `--follow-up` only when the message should wait until current work finishes. `--prompt @-` and `--text @-` read from stdin.";
