# Copy this file to ios/.env.testflight.local (gitignored)
# and fill in your local TestFlight credentials.

# App Store Connect API key ID (not secret)
ASC_KEY_ID=Z5L3J5H6YV

# Optional explicit path to .p8 API key file
# ASC_KEY_PATH=$HOME/.appstoreconnect/AuthKey_Z5L3J5H6YV.p8

# Preferred: store issuer UUID in a local file outside repo
ASC_ISSUER_ID_FILE=$HOME/.appstoreconnect/issuer_id

# Optional team override (defaults to AZAQMY4SPZ)
# OPPI_TEAM_ID=AZAQMY4SPZ
