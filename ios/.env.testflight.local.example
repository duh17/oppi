# Optional local TestFlight config (copy to .env.testflight.local)
# Caller-provided environment variables override values in .env.testflight.local.

# App Store Connect API key ID
ASC_KEY_ID=XXXXXXXXXX

# Preferred: file containing ASC issuer UUID
ASC_ISSUER_ID_FILE=$HOME/.appstoreconnect/issuer_id

# Optional direct issuer UUID (if not using ASC_ISSUER_ID_FILE)
# ASC_ISSUER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Optional explicit .p8 path (auto-detected if omitted)
# ASC_KEY_PATH=$HOME/.appstoreconnect/AuthKey_XXXXXXXXXX.p8

# Optional team override
# OPPI_TEAM_ID=YOUR_TEAM_ID
